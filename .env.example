# Porta do servidor
PORT=5000

# MongoDB Connection String
# Obtenha no MongoDB Atlas: https://cloud.mongodb.com
# Format: mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/DATABASE_NAME
MONGO_URI=mongodb+srv://SEU_USUARIO:SUA_SENHA@cluster.mongodb.net/wedding-app?retryWrites=true&w=majority

# Admin Code para verificações adicionais (opcional)
ADMIN_CODE=SEU-CODIGO-ADMIN-UNICO

# JWT Secret para autenticação
# IMPORTANTE: Use um valor aleatório e forte
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=gere-um-secret-aleatorio-forte-aqui

# JWT Expiration Time
JWT_EXPIRES_IN=7d

# Frontend URL (para CORS)
CLIENT_ORIGIN=https://seu-frontend.vercel.app

# ⚠️ IMPORTANTE: 
# 1. Copie este arquivo para .env
# 2. Substitua TODOS os valores de exemplo por valores reais
# 3. NUNCA commite o arquivo .env ao Git
# 4. Configure as mesmas variáveis no Vercel (Settings > Environment Variables)
